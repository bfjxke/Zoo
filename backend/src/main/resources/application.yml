server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/guardianeye_zoo?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 4321
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

python:
  agent:
    url: http://localhost:8000

tick:
  interval-seconds: 30
